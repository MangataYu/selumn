# Selume 功能改造 TODO

整理日期：2026-09-19；标签改造更新：2026-09-20。本文记录功能方向和已验证的实施进度。实施顺序为建议；标注“待定”的内容需在对应功能开始前确定。

目标：方便地收录自己在聊天、评论区等地方写过的话，在正文中用标签整理记录，并让本地模型辅助补充标题、标签和感受。

## 1. 正文标签与层级标签，替代分类

本次采用 `/` 表示层级，路径区分大小写；同一路径合并，父标签筛选包含全部子标签记录。保留编辑页原有标签位置，删除分类选择和独立分类管理入口。

- [x] 正文中输入 `#标签名`，再输入空格，自动形成可识别的标签，继续正常输入正文。
- [x] 输入 `#` 时在光标附近弹出已有标签候选；继续输入可筛选，支持选择历史标签或创建新标签。
- [x] 支持 `#工作/项目`、`#生活/运动`；新正文标签允许中文等 Unicode 字母、数字、组合符、下划线和连字符，路径不允许空段。既有含空格、标点的名称保留可读并可从候选复用。
- [x] 同一条记录支持多个标签；重复使用同一标签不产生重复关联。
- [x] 点击正文或列表标签查看相关记录；首页、搜索、时间线、图谱、管理列表和导出范围使用标签筛选，父标签包含子标签记录。
- [x] 将可完整解析父子关系的旧分类关联转换为标签路径；与既有同路径标签合并，保留记录修改时间和回收站关联。启动、旧库迁移及同步/备份恢复批次完成后幂等执行。
- [x] 保留编辑页标签行，移除分类按钮、分类管理入口及其路由注册。侧栏浏览标签，长按可重命名或删除标签及子标签。
- [x] 正文标记与记录标签随编辑、删除、撤销、重做和重命名更新；保留手工标签。顶部删除正文标签保留名称文字并支持撤销；自动化覆盖中文输入法组合状态、换行、粘贴、标题、代码及 URL 边界。
- [x] 智能助手以标签搜索、统计、新建和编辑日记；关闭旧分类工具入口，标签变化沿用原有确认预览。带标签的查询目前使用关键词检索，未运行语义检索。
- [x] 标签参与搜索、导入、导出、备份和同步；保留旧分类表、模型及载荷的读取兼容，Markdown 旧分类元数据导入后转为标签。
- [x] 使用依赖自带的 Isar 动态库完成旧库迁移集成测试；SQLite v1–v4 升级至 v5、备份恢复和迟到父分类冲突均有自动化覆盖。
- [ ] 真机验证中文输入法、旧库首次升级和跨设备同步/恢复。

兼容边界：没有记录的旧分类仍保存在兼容表中，不作为空标签显示。缺少父分类或循环关系的旧关联会原样保留，待数据完整后重试；未完成迁移期间的标签删除和重命名意图随记录保存、备份和同步，父分类补齐后不会恢复已移除的旧标签。新旧版本混用同步时，旧版单独重命名分类不能继续映射到新版已迁移的标签；建议同步设备一并升级。

验收场景：输入 `今天又改了方案 #工作/项目 ` 后形成标签；下一次输入 `#工` 能找到并复用它；点击“工作”或“工作/项目”都能找到这条记录，不再需要另选分类。

## 2. 从其他应用快捷记录文本

核心场景是保存自己在各种聊天、评论区写过的文字，减少切换和重复输入。

- [ ] Android 接收系统分享的纯文本，创建文字记录。
- [ ] Android 在支持系统选中文字菜单的地方提供“记到 Selume”入口，减少操作步骤。
- [ ] iOS 增加系统分享入口，完成分享扩展与主应用之间的文本交接及导入。
- [ ] 设计尽量简短的保存反馈；以保存原文和记录时间为基础，不要求用户每次填写标题、标签和情绪。
- [ ] 保留换行和原始文字；应用未启动、已启动、启用应用锁或正在初始化时，都能可靠接收，同一次操作只创建一条记录。
- [ ] 评估增加应用内“粘贴记录”，作为来源应用没有选中文字/分享入口时的补充方式。

平台限制：选中文字入口是否显示取决于来源应用。系统分享与复制粘贴是两条不同路径，不能承诺所有应用都具有相同入口。

## 3. 情绪与感受体系改造

已经明确的问题：现有 16 个选项混合了情绪、活动和身体状态；单个固定选项难以表达“烦、焦躁、闷、急”等感受。

- [ ] 将工作、学习、运动等主题从情绪中分离，纳入主题标签体系。
- [ ] 支持更细致、模糊和个人化的感受表达，保留用户写下的原词，不强制归成某个标准情绪。
- [ ] 设计感受录入方式。建议评估“常用词快捷选择 + 自由输入”，以及同一记录能否保留多个感受；具体形式待定。
- [ ] 区分“未记录/无法判断”和“平静”，避免缺少信息时自动算作平静。
- [ ] 制定旧情绪数据迁移规则：保留历史记录含义，不能把旧的“工作、运动”等值直接变成平静，也不能据此反推出当时的情绪。
- [ ] 同步调整编辑器、列表和时间线颜色、导出及情绪统计，验证与旧记录兼容。

验收场景：一条记录可以同时表达“主题是工作”和“感受是烦”；只写一个“闷”也可以保留为完整记录，不要求进一步解释。

## 4. 分享导入后的本地 AI 辅助整理

随着分类被标签取代，原先讨论的“AI 自动分类”统一改为“AI 推荐标签”。

- [ ] 分享导入后可自动调用已启用的本地模型，生成简短标题、提取关键词、推荐主题标签和感受。
- [ ] 优先复用已有标签，减少同义标签；是否允许模型直接创建新标签、创建层级标签，待定。
- [ ] 补齐标题、自由关键词和感受词所需的文本生成能力。当前 `MoodLlmEngine` 只从候选项中选一个，不能直接生成这些内容。
- [ ] 调整当前“主题优先、命中后不再分析情绪”的逻辑，让主题标签与感受能分别得到结果。
- [ ] 评估长文本策略：当前实现只读前 800 个字符，一次最多 26 个候选项；标签增多后需要候选筛选或其他适配。
- [ ] 采用“先保存原文、再补充整理结果”的建议流程；推理失败保留记录，晚到的结果不覆盖用户已修改的字段。
- [ ] 对短句和模糊表达保守处理：优先保留明确出现的感受词，无法判断可留空，不强制给每条记录补齐所有信息。
- [ ] 验证手机上的任务执行时机与中断恢复；iOS 分享扩展退出后不保证继续运行模型，必要时在主应用下次运行时完成。
- [ ] 用代表性中文短句、评论和长记录测试现有 Qwen3 0.6B INT8 的质量、耗时、内存与耗电，再决定是否需要更大模型。

验收场景：分享后原文立即被收存；模型可用时补充整理结果，用户可修改；模型不可用或被中断时，原文仍完整可读。

## 5. 开始实现前需确定的细节

- [x] 标签采用 `/` 层级路径、区分大小写、同路径合并；重命名更新子路径及正文，正文使用带标签属性的文本标记维护关联。
- [ ] 感受使用独立字段，还是使用带明确语义的标签；界面上需能区分主题与感受。
- [ ] AI 推荐标签是先展示建议还是自动应用；应用后是否作为正文中的标签显示，如何避免改写用户原话。
- [ ] 分享后是直接保存还是提供轻量预览；个人表达与收藏文字是否需要区分，避免把他人文字计入用户情绪统计。

## 实施顺序建议

1. 先确定标签与层级规则，完成正文标签交互和旧分类迁移。
2. 接入快捷记录；此项可与标签改造并行，先保证文字可靠保存。
3. 明确感受的表达与旧数据兼容方式，改造现有混合选项。
4. 基于新的标签和感受体系接入本地 AI，并进行手机实测。

来源应用识别、原文出处追踪、后台监听剪贴板、升级更大模型留作后续评估，不作为快捷记录第一版的必要条件。
